# Test Case: Missing pipeline key
# Expected Error: Missing required pipeline section

stages:
  - name: "build"

jobs:
  - name: "compile"
    stage: "build"
    image: "golang:1.21"
    script:
      - "make build"