# Generated by Helm

This file is produced by:

```bash
./scripts/render-k8s-manifests.sh
```

Edit the chart in `charts/e-team/` (and `charts/e-team/values.yaml`), then re-run the script and commit the updated `e-team.yaml`.
