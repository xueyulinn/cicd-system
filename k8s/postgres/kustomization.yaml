# kubectl apply -k k8s/postgres/
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: e-team

resources:
  - namespace.yaml
  - secret.yaml
  - service-headless.yaml
  - service.yaml
  - statefulset.yaml
  - migrate-job.yaml
