{{- if .Values.reportingService.enabled }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "e-team.reportingServiceName" . }}-config
  labels:
    {{- include "e-team.labels" . | nindent 4 }}
    app.kubernetes.io/component: reporting-service
data:
  PORT: {{ .Values.reportingService.service.port | quote }}
  DATABASE_URL: {{ include "e-team.databaseURL" . | quote }}
  SERVICE_NAME: reporting-service
  OTEL_SERVICE_NAME: reporting-service
  {{- if .Values.observability.enabled }}
  OTEL_EXPORTER_OTLP_ENDPOINT: {{ include "e-team.otelHTTPEndpoint" . | quote }}
  OTEL_EXPORTER_OTLP_PROTOCOL: http/protobuf
  {{- end }}
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "e-team.reportingServiceName" . }}
  labels:
    {{- include "e-team.labels" . | nindent 4 }}
    app.kubernetes.io/component: reporting-service
spec:
  replicas: {{ .Values.reportingService.replicaCount }}
  selector:
    matchLabels:
      {{- include "e-team.componentLabels" (dict "context" . "component" "reporting-service") | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "e-team.componentLabels" (dict "context" . "component" "reporting-service") | nindent 8 }}
      {{- with .Values.global.podAnnotations }}
      annotations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
    spec:
      {{- with .Values.global.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- if .Values.reportingService.waitForDatabase.enabled }}
      initContainers:
        - name: wait-for-report-db
          image: "{{ .Values.reportingService.waitForDatabase.image.repository }}:{{ .Values.reportingService.waitForDatabase.image.tag }}"
          imagePullPolicy: {{ .Values.reportingService.waitForDatabase.image.pullPolicy }}
          env:
            - name: PGHOST
              value: {{ include "e-team.databaseHost" . | quote }}
            - name: PGPORT
              value: {{ include "e-team.databasePort" . | quote }}
            - name: PGUSER
              value: {{ include "e-team.databaseUser" . | quote }}
            - name: PGDATABASE
              value: {{ include "e-team.databaseName" . | quote }}
            - name: PGPASSWORD
              value: {{ include "e-team.databasePassword" . | quote }}
          command:
            - sh
            - -c
            - |
              set -e
              echo "Waiting for Postgres..."
              until pg_isready -h "$PGHOST" -p "$PGPORT" -U "$PGUSER" -d "$PGDATABASE"; do
                sleep 2
              done
              echo "Waiting for migrations (pipeline_runs)..."
              until [ "$(psql -h "$PGHOST" -p "$PGPORT" -U "$PGUSER" -d "$PGDATABASE" -t -A -c \
                "SELECT EXISTS (SELECT 1 FROM information_schema.tables WHERE table_schema = 'public' AND table_name = 'pipeline_runs')::text" | tr -d '[:space:]')" = "true" ]; do
                sleep 2
              done
      {{- end }}
      containers:
        - name: reporting-service
          image: "{{ .Values.reportingService.image.repository }}:{{ .Values.reportingService.image.tag }}"
          imagePullPolicy: {{ .Values.reportingService.image.pullPolicy }}
          ports:
            - name: http
              containerPort: {{ .Values.reportingService.service.port }}
          envFrom:
            - configMapRef:
                name: {{ include "e-team.reportingServiceName" . }}-config
          readinessProbe:
            httpGet:
              path: /ready
              port: http
            initialDelaySeconds: 5
            periodSeconds: 10
            timeoutSeconds: 2
            failureThreshold: 3
          livenessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 10
            periodSeconds: 20
            timeoutSeconds: 2
            failureThreshold: 3
          resources:
            {{- toYaml .Values.reportingService.resources | nindent 12 }}
---
apiVersion: v1
kind: Service
metadata:
  name: {{ include "e-team.reportingServiceName" . }}
  labels:
    {{- include "e-team.labels" . | nindent 4 }}
    app.kubernetes.io/component: reporting-service
spec:
  selector:
    {{- include "e-team.componentLabels" (dict "context" . "component" "reporting-service") | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.reportingService.service.port }}
      targetPort: http
  type: ClusterIP
{{- end }}
